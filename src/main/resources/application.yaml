server:
  port: 3000
  servlet:
    context-path: /v1/api
spring:
  application:
    name: SpringVerse
  datasource:
    url: jdbc:postgresql://ep-small-water-aonyztvm-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_HUrIuOp9W1SF
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
